(function (Backbone, $) {

  var Tweet = window.Tweet = Backbone.Model.extend({

    initialize: function () {
      var that = this;
      // _.each(this.favoritedBy, function (userJSON, index, array) {
      //   array[index] = new User(userJSON);
      // });
    }
    
  });

}(Backbone, jQuery));